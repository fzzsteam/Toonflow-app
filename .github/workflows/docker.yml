name: Build and Push Docker Image

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "版本号（留空则读 package.json）"
        required: false
        type: string

jobs:
  docker:
    runs-on: ubuntu-latest
    name: 构建并推送 Docker 镜像

    permissions:
      contents: read
      packages: write

    steps:
      - name: 检出后端代码
        uses: actions/checkout@v4

      - name: 检出前端代码（Toonflow-web）
        run: |
          # 若 Toonflow-web 为私有仓库，需在仓库 Secrets 中配置 GITEE_TOKEN
          if [ -n "${{ secrets.GITEE_TOKEN }}" ]; then
            git clone https://oauth2:${{ secrets.GITEE_TOKEN }}@gitee.com/shenzhen-fangzhi-zhisheng/Toonflow-web.git ../Toonflow-web
          else
            git clone https://gitee.com/shenzhen-fangzhi-zhisheng/Toonflow-web.git ../Toonflow-web
          fi

      - name: 获取版本号
        id: version
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            echo "tag=v${{ inputs.version }}" >> $GITHUB_OUTPUT
          else
            VERSION=$(node -p "require('./package.json').version")
            echo "tag=v$VERSION" >> $GITHUB_OUTPUT
          fi

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: jiawei666
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并推送镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          build-contexts: |
            web=../Toonflow-web
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/jiawei666/toonflow:${{ steps.version.outputs.tag }}
            ghcr.io/jiawei666/toonflow:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 输出结果
        run: |
          echo "✅ 推送完成"
          echo "镜像：ghcr.io/jiawei666/toonflow:${{ steps.version.outputs.tag }}"
          echo "镜像：ghcr.io/jiawei666/toonflow:latest"
